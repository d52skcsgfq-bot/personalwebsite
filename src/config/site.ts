// Site configuration
// ---------------------------------------------------------------
// Change `theme` to switch the site's look. That's the only line
// you need to edit. Available themes are defined in src/styles/global.css
// as `:root[data-theme='...']` blocks.
//
//   'portfolio' — dark navy + emerald accent (personal portfolio look)
//   'fusion5'   — dark violet + coral accent (Fusion5 corporate brand)
//
export type Theme = 'portfolio' | 'fusion5';

export const theme: Theme = 'fusion5';
